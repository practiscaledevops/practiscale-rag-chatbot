# DEMO MODE template — copy to .env.local to run the chatbot with NO Supabase and
# NO Brain key (in-memory fixtures + canned AI stream). For a real deployment,
# use .env.example instead.
#   cp .env.demo .env.local && npm run dev
DEMO_MODE=1
NEXT_PUBLIC_DEMO_MODE=1

# Harmless placeholders so module-level client construction never throws.
NEXT_PUBLIC_SUPABASE_URL=https://demo.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=demo-anon-key
SUPABASE_SERVICE_ROLE_KEY=demo-service-role-key
BRAIN_API_URL=http://localhost:3000
BRAIN_API_KEY=psk_live_demo
APP_SECRET=demo-app-secret
DEFAULT_MODEL_TIER=recommended
